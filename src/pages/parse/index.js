import Layout from "@/components/Layout";

const parse = () => {
    return (
        <Layout>
        <div className="bg-red-500 text-white">
            Parse Unstructreds data
        </div>
        </Layout>
    );
}

export default parse;